function getUser(id) {
    //variable pour dire si on est en mock data ou en api
    //il cherche dans les mocks data le user avec lid
return new UserModel(info , activity, averageSession, performance); 
}